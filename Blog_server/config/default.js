const rootConfig = {
    port: 3000,
    database:{
        //连接数据库
        HOST: "localhost",
        USER: "root",
        PASSWORD: "",
        DB: "Blog_Project"
    }
}

module.exports = rootConfig;